---
import { coolSymbolCategories } from '@/utils/coolSymbols';
import Layout from '@/layouts/Layout.astro';

const faqs = [
  {
    question: "How can I use these symbols?",
    answer: "Simply click on any symbol to copy it to your clipboard, then paste it wherever you want to use it."
  },
  {
    question: "Will these symbols work everywhere?",
    answer: "Most modern platforms support these Unicode symbols, but some older systems might display them differently."
  }
];
const lang = "es";
const title = "Símbolos Geniales - Copiar y Pegar Símbolos | InvisibleSymbol";
const description = "Copia y pega símbolos geniales, símbolos invisibles y caracteres especiales de nuestra colección.";
---
<Layout title={title} description={description}>
  <div class="mx-auto sm:space-y-8">
    <header class="max-w-4xl mx-auto px-6 py-6">
      <h1 class="text-4xl sm:text-3xl font-bold text-center text-gray-900 mb-2 sm:mb-4">Símbolos Geniales</h1>
      <p class="text-md md:text-lg text-gray-600 text-center px-2 sm:px-0">Los símbolos geniales son caracteres e íconos únicos que mejoran el interés visual y expresan emociones o ideas utilizando símbolos únicos. Se utilizan ampliamente en redes sociales, aplicaciones de mensajería y sitios web para hacer el contenido más atractivo.</p>
    </header>

    <section class="text-center px-6">
      <h2 class="text-xl sm:text-2xl font-bold mb-2 sm:mb-4 text-gray-900">Cómo Funciona</h2>
      <p class="mb-4 sm:mb-6 text-md md:text-lg text-gray-600 text-center">
          Toca cualquier símbolo para ver sus detalles, luego toca 
          <span class="text-blue-500 font-semibold">"Copiar al portapapeles"</span> para copiarlo.
      </p>
    </section>
    
    <section class="space-y-4 sm:space-y-6 px-6 ">
      {coolSymbolCategories.map(category => (
        <div class=" rounded-lg shadow-lg p-6 sm:p-6">
          <h3 class="text-lg sm:text-xl font-semibold text-gray-900 mb-3 sm:mb-4">{category.name}</h3>
          <div class="grid grid-cols-4 xs:grid-cols-4 sm:grid-cols-6 md:grid-cols-8 gap-2 sm:gap-4">
            {category.symbols.map(symbol => (
              <div class="relative group items-center">
                <button
                  class="w-full aspect-square flex items-center justify-center text-5xl md:text-6xl lg:text-7xl bg-gray-100 rounded-lg hover:bg-gray-100 transition-all duration-200"
                  aria-label={`Copiar ${symbol.name}`}
                >
                  {symbol.char}
                </button>
                
                <!-- Tooltip amigable para móviles -->
                <div class="border-black opacity-0 bg-black invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 absolute z-10 w-36 sm:w-48 p-3 sm:p-4  rounded-lg shadow-lg -translate-x-1/2 left-1/2 top-full mt-2">
                  <div class="text-4xl sm:text-4xl text-center mb-2">{symbol.char}</div>
                  <div class="text-xs sm:text-sm text-gray-600 text-center mb-2">{symbol.name}</div>
                  <button
                    type="button"
                    class="copy-btn w-full py-1.5 sm:py-2 px-3 sm:px-4 bg-blue-500 text-white text-sm rounded hover:bg-black transition-colors duration-200"
                    data-symbol={symbol.char}
                  >
                    Copiar
                  </button>
                  <div class="copied-indicator hidden absolute top-2 right-2 text-green-500 text-xs sm:text-sm font-medium">
                    ¡Copiado!
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </section>

    <section class="px-6">
      <div class="max-w-6xl mx-auto shadow-md rounded-lg p-4 sm:p-6 py-16">
        <h2 class="text-xl sm:text-2xl font-semibold mb-3 sm:mb-4">Tipos de Símbolos Geniales:</h2>
        <ul class="list-disc pl-6 mb-6">
            <li class="mb-2"><span class="font-bold">Emojis:</span> Iconos pictóricos que representan emociones, objetos o conceptos. Por ejemplo: 😊, 🚀, 🎉.</li>
            <li class="mb-2"><span class="font-bold">Caracteres Unicode:</span> Caracteres especiales del Estándar Unicode, como estrellas (★), corazones (♥) o flechas (→).</li>
            <li class="mb-2"><span class="font-bold">Arte ASCII:</span> Diseños creativos hechos con caracteres estándar del teclado, a menudo formando imágenes o patrones.</li>
            <li class="mb-2"><span class="font-bold">Símbolos de Moneda:</span> Representaciones de varias monedas, como $, €, ¥ o £.</li>
            <li class="mb-2"><span class="font-bold">Símbolos Matemáticos:</span> Caracteres utilizados en expresiones matemáticas, como ∑ (sumatoria), ∞ (infinito) o √ (raíz cuadrada).</li>
        </ul>

        <h2 class="text-2xl font-semibold mb-4">Usando Símbolos Geniales:</h2>
        <p class="mb-4 text-gray-600">
            Incorporar símbolos geniales en tu contenido digital puede hacerlo más atractivo y transmitir mensajes de manera más efectiva. Aquí hay algunos consejos:
        </p>
        <ul class="list-disc pl-6 mb-6">
            <li class="mb-2"><span class="font-bold">Copiar y Pegar:</span> Muchos sitios web y aplicaciones te permiten copiar símbolos directamente y pegarlos en tu contenido.</li>
            <li class="mb-2"><span class="font-bold">Atajos de Teclado:</span> Algunos símbolos se pueden insertar usando atajos de teclado. Por ejemplo, en Windows, puedes usar códigos <code class="bg-gray-200 px-1 py-0.5 rounded text-sm">Alt</code>; en Mac, puedes usar la tecla <code class="bg-gray-200 px-1 py-0.5 rounded text-sm">Option</code> con otras teclas.</li>
            <li class="mb-2"><span class="font-bold">Herramientas de Mapa de Caracteres:</span> Los sistemas operativos a menudo incluyen utilidades de mapa de caracteres que te permiten seleccionar e insertar caracteres especiales.</li>
        </ul>

        <h2 class="text-2xl font-semibold mb-4">Consideraciones:</h2>
        <ul class="list-disc pl-6">
            <li class="mb-2"><span class="font-bold">Compatibilidad:</span> Asegúrate de que los símbolos que uses sean compatibles en diferentes dispositivos y plataformas para mantener la consistencia.</li>
            <li class="mb-2"><span class="font-bold">Legibilidad:</span> Usa símbolos con moderación para evitar el desorden y asegurar que tu mensaje sea claro.</li>
            <li class="mb-2"><span class="font-bold">Diferencias Culturales:</span> Ten en cuenta que algunos símbolos pueden tener diferentes significados en diferentes culturas.</li>
        </ul>
      </div>
    </section>
  </div>
</Layout>




<script>
  // Wait for the DOM to be fully loaded
  document.addEventListener('DOMContentLoaded', () => {
    // Add click event listeners to all copy buttons
    document.querySelectorAll('.copy-btn').forEach(button => {
      button.addEventListener('click', async (e) => {
        // Get the symbol to copy
        const symbol = (e.target as HTMLElement).dataset.symbol;
        
        try {
          // Try to copy the text
          await navigator.clipboard.writeText(symbol);
          
          // Show the copied indicator
          const tooltipContainer = (e.target as Element).closest('.relative');
          const copiedIndicator = tooltipContainer.querySelector('.copied-indicator');
          
          // Show the copied indicator
          copiedIndicator.classList.remove('hidden');
          
          // Hide it after animation
          setTimeout(() => {
            copiedIndicator.classList.add('hidden');
          }, 2000);
          
        } catch (err) {
          console.error('Failed to copy text: ', err);
        }
      });
    });
  });
  
  // Close tooltip when clicking outside
  document.addEventListener('click', (event) => {
    const tooltips = document.querySelectorAll('.group');
    tooltips.forEach(tooltip => {
      if (!tooltip.contains(event.target as Node)) {
        const copiedIndicator = tooltip.querySelector('.copied-indicator');
        if (copiedIndicator) {
          copiedIndicator.classList.add('hidden');
        }
      }
    });
  });
</script>


