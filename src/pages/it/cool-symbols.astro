---
import Layout from '@/layouts/Layout.astro';
import { coolSymbolCategories } from '@/utils/coolSymbols';

const faqs = [
  {
    question: "How can I use these symbols?",
    answer: "Simply click on any symbol to copy it to your clipboard, then paste it wherever you want to use it."
  },
  {
    question: "Will these symbols work everywhere?",
    answer: "Most modern platforms support these Unicode symbols, but some older systems might display them differently."
  }
];
const lang = "it";
const title = "Simbol Keren - Salin dan Tempel Simbol | InvisibleSymbol";
const description = "Salin dan tempel simbol keren, simbol tak terlihat, dan karakter khusus dari koleksi kami.";
---
<Layout title={title} description={description}>
  <div class="mx-auto space-y-8 sm:space-y-8">
    <header class="max-w-4xl mx-auto px-6 py-6">
      <h1 class="text-4xl sm:text-3xl font-bold text-center text-gray-900 mb-2 sm:mb-4">Simboli Fantastici</h1>
      <p class="text-md md:text-lg text-gray-600 text-center px-2 sm:px-0">I simboli fantastici sono caratteri e icone unici che migliorano l'interesse visivo ed esprimono emozioni o idee utilizzando simboli unici. Sono ampiamente utilizzati sui social media, nelle app di messaggistica e sui siti web per rendere i contenuti più accattivanti.</p>
    </header>

    <section class="text-center px-6">
      <h2 class="text-xl sm:text-2xl font-bold mb-2 sm:mb-4 text-gray-900">Come Funziona</h2>
      <p class="mb-4 sm:mb-6 text-md md:text-lg text-gray-600 text-center">
          Tocca qualsiasi simbolo per vedere i dettagli, quindi tocca 
          <span class="text-blue-500 font-semibold">"Copia negli Appunti"</span> per copiarlo.
      </p>
    </section>
    
    <section class="space-y-4 sm:space-y-6 px-6">
      {coolSymbolCategories.map(category => (
        <div class="bg-white rounded-lg shadow-lg p-6 sm:p-6">
          <h3 class="text-lg sm:text-xl font-semibold text-gray-900 mb-3 sm:mb-4">{category.name}</h3>
          <div class="grid grid-cols-4 xs:grid-cols-4 sm:grid-cols-6 md:grid-cols-8 gap-2 sm:gap-4">
            {category.symbols.map(symbol => (
              <div class="relative group items-center">
                <button
                  class="w-full aspect-square flex items-center justify-center text-5xl md:text-6xl lg:text-7xl bg-gray-100 rounded-lg hover:bg-gray-100 transition-all duration-200"
                  aria-label={`Copia ${symbol.name}`}
                >
                  {symbol.char}
                </button>
                
                <!-- Tooltip mobile-friendly -->
                <div class="border-black opacity-0 bg-black invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 absolute z-10 w-36 sm:w-48 p-3 sm:p-4 bg-white rounded-lg shadow-lg -translate-x-1/2 left-1/2 top-full mt-2">
                  <div class="text-4xl sm:text-4xl text-center mb-2">{symbol.char}</div>
                  <div class="text-xs sm:text-sm text-gray-600 text-center mb-2">{symbol.name}</div>
                  <button
                    type="button"
                    class="copy-btn w-full py-1.5 sm:py-2 px-3 sm:px-4 bg-blue-500 text-white text-sm rounded hover:bg-black transition-colors duration-200"
                    data-symbol={symbol.char}
                  >
                    Copia
                  </button>
                  <div class="copied-indicator hidden absolute top-2 right-2 text-green-500 text-xs sm:text-sm font-medium">
                    Copiato!
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </section>

    <section class="px-6">
      <div class="max-w-6xl mx-auto bg-white shadow-md rounded-lg p-4 sm:p-6 py-16">
        <h2 class="text-xl sm:text-2xl font-semibold mb-3 sm:mb-4">Tipi di Simboli Fantastici:</h2>
        <ul class="list-disc pl-6 mb-6">
            <li class="mb-2"><span class="font-bold">Emoji:</span> Icone pittografiche che rappresentano emozioni, oggetti o concetti. Ad esempio: 😊, 🚀, 🎉.</li>
            <li class="mb-2"><span class="font-bold">Caratteri Unicode:</span> Caratteri speciali dallo standard Unicode, come stelle (★), cuori (♥) o frecce (→).</li>
            <li class="mb-2"><span class="font-bold">Arte ASCII:</span> Design creativi realizzati con caratteri standard della tastiera, spesso formando immagini o motivi.</li>
            <li class="mb-2"><span class="font-bold">Simboli di Valuta:</span> Rappresentazioni di varie valute, come $, €, ¥ o £.</li>
            <li class="mb-2"><span class="font-bold">Simboli Matematici:</span> Caratteri utilizzati nelle espressioni matematiche, come ∑ (somma), ∞ (infinito) o √ (radice quadrata).</li>
        </ul>

        <h2 class="text-2xl font-semibold mb-4">Utilizzare i Simboli Fantastici:</h2>
        <p class="mb-4 text-gray-600">
            Incorporare simboli fantastici nei tuoi contenuti digitali può renderli più accattivanti e trasmettere il messaggio in modo più efficace. Ecco alcuni suggerimenti:
        </p>
        <ul class="list-disc pl-6 mb-6">
            <li class="mb-2"><span class="font-bold">Copia e Incolla:</span> Molti siti web e app ti permettono di copiare direttamente i simboli e incollarli nei tuoi contenuti.</li>
            <li class="mb-2"><span class="font-bold">Scorciatoie da Tastiera:</span> Alcuni simboli possono essere inseriti utilizzando scorciatoie da tastiera. Ad esempio, su Windows, puoi usare il codice <code class="bg-gray-200 px-1 py-0.5 rounded text-sm">Alt</code>; su Mac, puoi usare il tasto <code class="bg-gray-200 px-1 py-0.5 rounded text-sm">Option</code> con altri tasti.</li>
            <li class="mb-2"><span class="font-bold">Strumento Mappa Caratteri:</span> I sistemi operativi spesso includono un'utilità mappa caratteri che ti permette di selezionare e inserire caratteri speciali.</li>
        </ul>

        <h2 class="text-2xl font-semibold mb-4">Considerazioni:</h2>
        <ul class="list-disc pl-6">
            <li class="mb-2"><span class="font-bold">Compatibilità:</span> Assicurati che i simboli che utilizzi siano compatibili su vari dispositivi e piattaforme per mantenere la coerenza.</li>
            <li class="mb-2"><span class="font-bold">Leggibilità:</span> Usa i simboli con saggezza per evitare confusione e assicurarti che il tuo messaggio sia chiaro.</li>
            <li class="mb-2"><span class="font-bold">Differenze Culturali:</span> Ricorda che alcuni simboli possono avere significati diversi in culture diverse.</li>
        </ul>
      </div>
    </section>
  </div>
</Layout>




<script>
  // Wait for the DOM to be fully loaded
  document.addEventListener('DOMContentLoaded', () => {
    // Add click event listeners to all copy buttons
    document.querySelectorAll('.copy-btn').forEach(button => {
      button.addEventListener('click', async (e) => {
        // Get the symbol to copy
        const symbol = (e.target as HTMLElement).dataset.symbol;
        
        try {
          // Try to copy the text
          await navigator.clipboard.writeText(symbol);
          
          // Show the copied indicator
          const tooltipContainer = (e.target as Element).closest('.relative');
          const copiedIndicator = tooltipContainer.querySelector('.copied-indicator');
          
          // Show the copied indicator
          copiedIndicator.classList.remove('hidden');
          
          // Hide it after animation
          setTimeout(() => {
            copiedIndicator.classList.add('hidden');
          }, 2000);
          
        } catch (err) {
          console.error('Failed to copy text: ', err);
        }
      });
    });
  });
  
  // Close tooltip when clicking outside
  document.addEventListener('click', (event) => {
    const tooltips = document.querySelectorAll('.group');
    tooltips.forEach(tooltip => {
      if (!tooltip.contains(event.target as Node)) {
        const copiedIndicator = tooltip.querySelector('.copied-indicator');
        if (copiedIndicator) {
          copiedIndicator.classList.add('hidden');
        }
      }
    });
  });
</script>
